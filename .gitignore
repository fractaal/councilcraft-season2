# Built artifacts — don't commit these
*.mrpack
*.log
bootstrap.log
additions.log

# Local packwiz cache (if any)
.cache/

# Mod jars belong in Modrinth/CF CDN, never here.
# (scripts/packwiz-installer-bootstrap.jar is tooling, not a mod — keep it.)
mods/*.jar
.codex
